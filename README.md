# nachidev

Placeholder site for [nachidev.com.ar](https://nachidev.com.ar).

Static single-page site in `public/`. Auto-deploys to Cloudflare Pages on every push to `main` via GitHub Actions (`.github/workflows/deploy.yml`).
